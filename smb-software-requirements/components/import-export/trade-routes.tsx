"use client"

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export function TradeRoutes() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Trade Routes Map</CardTitle>
      </CardHeader>
      <CardContent>{"Interactive trade-route visualisations will be added soon."}</CardContent>
    </Card>
  )
}

export default TradeRoutes
