"use client"

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card"

export function ProductionTracking() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Production Tracking</CardTitle>
      </CardHeader>
      <CardContent>{"Coming soon — real-time production KPIs will appear here."}</CardContent>
    </Card>
  )
}

export default ProductionTracking // optional default export
